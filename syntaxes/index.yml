name: SPWN
scopeName: source.spwn

includes:
  - comments
  - literals
  - keywords
  - brackets
  - other

patterns:
  - include: '#root'

repository:
    root:
        patterns:
          - include: '#comments'
          - include: '#literals'
          - include: '#keywords'
          - include: '#type-identifier'
          - include: '#punctuation'
          - include: '#brackets'