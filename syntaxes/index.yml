name: SPWN
scopeName: source.spwn

includes:
  - comments
  - literals
  - keywords

patterns:
  - include: '#root'

repository:
    root:
        patterns:
          - include: '#comments'
          - include: '#literals'
          - include: '#keywords'